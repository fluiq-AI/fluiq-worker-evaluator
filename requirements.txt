# Text judge calls go through polygate, Fluiq's own unified LLM client — one
# wire shape plus key rotation + backoff. The pin is >=0.3 because judge.py
# routes 21 providers: 0.2 shipped only four adapters, so an older resolve would
# accept a Groq or Z.AI judge and then fail at call time with UnsupportedProvider.
polygate>=0.3
# The native provider SDKs below are retained only for the multimodal/vision
# judge path, which polygate's text-only message shape can't express across all
# providers (Gemini especially). If vision moves off the SDKs, drop these.
anthropic>=0.34
google-genai>=0.3
openai>=1.40
pydantic>=2.6
aiokafka>=0.10
clickhouse-connect>=0.7
python-dotenv>=1.0
asyncpg>=0.29
fluiq

aiohttp
# Used by the fluiq LLM-judge provider (jobs/.../judge.py _call_fluiq).
# Previously present only transitively via sentence-transformers; now that the
# security deps are gone (moved to fluiq-workers/security), pin it directly.
requests>=2.31

# BYOK: unseal customer provider credentials (see jobs/helper/crypto.py).
boto3
cryptography
